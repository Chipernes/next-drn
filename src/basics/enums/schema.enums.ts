export enum Role {
  SERVICE = 'SERVICE',
  KITCHEN = 'KITCHEN',
  ADMINISTRATION = 'ADMINISTRATION',
}
export enum StatusOrder {
  NEW = 'NEW',
  PROCESSING = 'PROCESSING',
  DONE = 'DONE',
  CANCELLED = 'CANCELLED',
}

export enum StatusDish {
  NEW = 'NEW',
  COOKING = 'COOKING',
  DONE = 'DONE',
  SERVED = 'SERVED',
  CANCELLED = 'CANCELLED',
}
